# Continuous Network Ping Monitor

Written because my ISP in Prague claimed there were "no drops in the system."

A bash script that continuously monitors network ping and approximates download throughput to log actual uptime degradation.

**Usage:**

```bash
./network-ping-monitor.sh
```
